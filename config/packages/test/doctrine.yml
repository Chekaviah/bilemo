doctrine:
    dbal:
        driver: pdo_sqlite
        dbname: 'bilemo'
        path:  "%kernel.project_dir%/var/data.db"
        memory: true
        url:  "sqlite:///%kernel.project_dir%/var/data.db"
